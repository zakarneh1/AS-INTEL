interface WithVisualEditsOptions {
    /** Override automatic dev detection. Default: `process.env.NODE_ENV !== "production"` */
    enableVisualEdits?: boolean;
    /** Tailwind CDN URL passed to VisualEditsWebpackPlugin. Default: "https://cdn.tailwindcss.com" */
    tailwindCdn?: string | false;
}
/**
 * Wraps an existing craco config to add visual editing capabilities.
 *
 * Automatically:
 * 1. Adds the babel metadata plugin (dev only)
 * 2. Adds dev server middleware (/ping, /edit-file)
 * 3. Adds webpack plugin (emits overlay.js, injects <script> into HTML)
 */
declare function withVisualEdits(cracoConfig: any, options?: WithVisualEditsOptions): any;

export { type WithVisualEditsOptions, withVisualEdits as default, withVisualEdits };
