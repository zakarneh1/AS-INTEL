// src/types/changes.ts
function getPendingChangeKey(elementId, arrayIndex) {
  if (arrayIndex !== void 0 && arrayIndex !== null) {
    return `${elementId}:${arrayIndex}`;
  }
  return elementId;
}
export {
  getPendingChangeKey
};
